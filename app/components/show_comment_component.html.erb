<% comment=@comment %>
<div id="<%= dom_id comment%>">
  <%= render(CommentComponent.new(comment: comment)) %>
  <% comment.comments.each do |sub_comment|%>
  <div class="sub_comment ml-4" id="<%= dom_id sub_comment%>">
    <%= render(CommentComponent.new(comment: sub_comment)) %>
  </div>
  <% end %>
</div>
