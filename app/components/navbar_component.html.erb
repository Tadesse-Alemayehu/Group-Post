
<nav class="bg-white px-2 sm:px-4 py-2.5 bg-slate-400 dark:bg-gray-900 fixed w-full z-20 top-0 left-0 border-b border-gray-200 dark:border-gray-600" data-controller="theme">
  <div class="container flex flex-wrap items-center justify-between mx-auto">
    <div>
            <a href="#" class="flex items-center" id="dark" data-theme-target="dark" data-action="theme#toggleTheme">
      <i class="fa-solid fa-moon" ></i>
  </a>
        <a href="#" class="flex items-center"  id="light" data-theme-target="light"  data-action="theme#toggleTheme">
      <i class="fa-solid fa-sun text-white" ></i>
  </a>
    </div>
  <div class="flex md:order-2">
    <%= link_to 'groups', user_path(current_user), class: "overlay-btn blue bg-sky-500 text-blue-50 mr-2" %>

<form class="" method="post" action="/users/sign_out"><input type="hidden" name="_method" value="delete" autocomplete="off">
<button data-trubo="false" class="text-pink-200 bg-slate-500 p-3 rounded-lg" type="submit">Logout</button><input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>" autocomplete="off"></form>
  </div>

  </div>
</nav>



